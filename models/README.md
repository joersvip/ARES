# ARES Models

Holds data schema validations, machine learning classifiers, neural engines, or server domain schemas.

## Categories
- `domain/`: Pure data schemas (Drizzle, Pydantic, or Room entities)
- `intelligence/`: ML inference rules, neural models, or heuristics for cyber threat categorization
