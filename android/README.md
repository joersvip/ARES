# ARES Native Android App

A native Android client for the ARES platform, engineered in Kotlin using Jetpack Compose.

## Directory Structure
- `app/src/main/java/`: Core Kotlin codebase (ViewModel, Domain, UI layer)
- `app/src/main/res/`: UI XML resources and design tokens
- `gradle/`: Gradle wrappers and build scripts
