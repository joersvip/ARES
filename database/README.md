# ARES Database Configuration

Contains schema specifications, initialization files, and migrations for the ARES relational database engine (PostgreSQL).

## Core Files
- `schema.sql`: Primary structural tables for threats, endpoint records, alerts, and plugin configurations.
- `migrations/`: Versioned migration logs for production schema upgrades.
